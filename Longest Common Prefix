class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        i = 0

        while i < len(strs[0]):
            for word in strs:
                if i >= len(word) or strs[0][i] != word[i]:
                    return strs[0][:i]
            i += 1

        return strs[0]
